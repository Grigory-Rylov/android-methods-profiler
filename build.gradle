plugins {
    id 'idea'
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url 'https://jitpack.io' }
    }
}
subprojects {
    //Put instructions for each sub project
}
